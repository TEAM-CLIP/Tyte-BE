spring:
  profiles:
    group:
      dev:
        - security-local
        - persistence
      local:
        - security-local
        - persistence-local
      test:
        - security-local
        - persistence-test
    active: local
  management:
    endpoints:
      web:
        exposure:
          include: health
    endpoint:
      health:
        show-details: always